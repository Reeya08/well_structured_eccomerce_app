class CategoryModel {
  final String categoryName;
  final String categoryImage;
  CategoryModel({required this.categoryImage, required this.categoryName,});

}
/* Model : the custom data types that does not exist in flutter so we create or own data type that stores the
data according to our need
 */
